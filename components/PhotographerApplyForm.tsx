"use client";import{useState}from'react';import Link from'next/link';export function PhotographerApplyForm(){const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch('/api/lens-life/photographer/apply',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({displayName:f.get('displayName'),phone:f.get('phone'),homePostcode:f.get('postcode'),yearsExperience:f.get('years')?Number(f.get('years')):undefined,portfolioUrl:f.get('portfolioUrl'),bio:f.get('bio')})});const j=await r.json();setBusy(false);if(r.status===401){setMsg('Sign in or create an account first.');return}setMsg(r.ok?'Application created. Open your photographer portal to complete services, areas and compliance documents.':j.error||'Unable to apply')}return <form className="form" onSubmit={submit}><h3>Apply to join Lens Life</h3><label>Display name<input name="displayName" required/></label><label>Phone<input name="phone"/></label><label>Home postcode<input name="postcode" required/></label><label>Years experience<input name="years" type="number" min="0" max="80"/></label><label>Portfolio URL<input name="portfolioUrl" type="url"/></label><label>About your work<textarea name="bio" rows={4}/></label><button disabled={busy}>{busy?'Submitting…':'Create photographer application'}</button>{msg&&<p>{msg} {msg.startsWith('Sign')&&<Link href="/auth/login?next=/photography/photographers/apply">Sign in</Link>}</p>}</form>}
