"use client";import{useState}from'react';export function BusinessEnquiryForm(){const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const r=await fetch('/api/lens-life/business/enquiry',{method:'POST',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await r.json();setBusy(false);setMsg(r.ok?'Thanks. Your business enquiry has been added to the Lens Life account pipeline.':j.error||'Unable to submit enquiry')}return <form className="form" onSubmit={submit}><h3>Discuss a business account</h3><label>Organisation<input name="organisation" required/></label><label>Your name<input name="contactName" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone"/></label><label>Business type<select name="segment"><option value="property">Property</option><option value="corporate">Corporate / teams</option><option value="education">School / nursery</option><option value="hospitality">Hospitality</option><option value="other">Other</option></select></label><label>What do you need?<textarea name="message" rows={4}/></label><button disabled={busy}>{busy?'Submitting…':'Send business enquiry'}</button>{msg&&<p>{msg}</p>}</form>}
