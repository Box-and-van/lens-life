import './globals.css'; export const metadata={title:'Lens Life | Managed Photography',description:'Vetted photographers, managed from booking to delivery.'}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
