import { Shell } from '@/components/Shell'; import { login,signup } from './actions';export default async function Page({searchParams}:{searchParams:Promise<{next?:string,error?:string}>}){const q=await searchParams;return <Shell title="Sign in"><form className="form"><input type="hidden" name="next" value={q.next||'/photography/customer'}/><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{q.error&&<p className="error">{q.error}</p>}<div style={{display:'flex',gap:10}}><button formAction={login}>Sign in</button><button formAction={signup}>Create account</button></div></form></Shell>}
