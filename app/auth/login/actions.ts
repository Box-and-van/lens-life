"use server";import { redirect } from 'next/navigation';import { createClient } from '@/lib/supabase/server';
function safeNext(v:FormDataEntryValue|null){const s=String(v||'/photography/customer');return s.startsWith('/')&&!s.startsWith('//')?s:'/photography/customer'}
export async function login(fd:FormData){const s=await createClient();const email=String(fd.get('email')||'');const password=String(fd.get('password')||'');const next=safeNext(fd.get('next'));const {error}=await s.auth.signInWithPassword({email,password});if(error)redirect(`/auth/login?error=${encodeURIComponent(error.message)}&next=${encodeURIComponent(next)}`);redirect(next)}
export async function signup(fd:FormData){const s=await createClient();const email=String(fd.get('email')||'');const password=String(fd.get('password')||'');const next=safeNext(fd.get('next'));const base=process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000';const {error}=await s.auth.signUp({email,password,options:{emailRedirectTo:`${base}/auth/confirm?next=${encodeURIComponent(next)}`}});if(error)redirect(`/auth/login?error=${encodeURIComponent(error.message)}&next=${encodeURIComponent(next)}`);redirect('/auth/login?error='+encodeURIComponent('Check your email to confirm your account.'))}
