import { NextResponse } from 'next/server';import { adminDb } from '@/lib/supabase/admin';export const dynamic='force-dynamic';export async function GET(){try{const {error}=await adminDb().from('ll_runtime_config').select('id').limit(1);if(error)throw error;return NextResponse.json({ok:true,database:true})}catch(e){return NextResponse.json({ok:false,database:false},{status:503})}}
