import {NextResponse} from 'next/server';import{z}from'zod';import{apiActor}from'@/lib/api';import{adminDb}from'@/lib/supabase/admin';const schema=z.object({firstName:z.string().max(100).optional(),lastName:z.string().max(100).optional(),phone:z.string().max(50).optional(),marketingOptIn:z.boolean().optional()});export async function PATCH(req:Request){try{const a=await apiActor('customer');const x=schema.parse(await req.json());const patch:any={};if(x.firstName!==undefined)patch.first_name=x.firstName;if(x.lastName!==undefined)patch.last_name=x.lastName;if(x.phone!==undefined)patch.phone=x.phone;if(x.marketingOptIn!==undefined)patch.marketing_opt_in=x.marketingOptIn;const{data,error}=await adminDb().from('ll_customers').update(patch).eq('id',a.customerId!).select('id,email,first_name,last_name,phone,marketing_opt_in').single();if(error)throw error;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Request failed'},{status:400})}}
