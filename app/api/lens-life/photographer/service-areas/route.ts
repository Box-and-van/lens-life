import { NextResponse } from 'next/server';import { z } from 'zod';import { apiActor } from '@/lib/api';import { adminDb } from '@/lib/supabase/admin';const schema=z.object({marketId:z.string().uuid().optional(),postcodePrefix:z.string().optional(),radiusMiles:z.number().positive().max(150).optional(),notes:z.string().optional()});export async function POST(req:Request){try{const a=await apiActor('photographer');const x=schema.parse(await req.json());const {data,error}=await adminDb().from('ll_photographer_service_areas').insert({photographer_id:a.photographerId,market_id:x.marketId,postcode_prefix:x.postcodePrefix,radius_miles:x.radiusMiles,travel_notes:x.notes}).select().single();if(error)throw error;return NextResponse.json(data,{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Request failed'},{status:400})}}
