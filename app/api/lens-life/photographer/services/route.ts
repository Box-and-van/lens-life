import { NextResponse } from 'next/server';import { z } from 'zod';import { apiActor } from '@/lib/api';import { adminDb } from '@/lib/supabase/admin';const schema=z.object({serviceOfferingId:z.string().uuid(),enabled:z.boolean().default(true),experienceLevel:z.string().optional()});export async function POST(req:Request){try{const a=await apiActor('photographer');const x=schema.parse(await req.json());const {data,error}=await adminDb().from('ll_photographer_services').upsert({photographer_id:a.photographerId,service_offering_id:x.serviceOfferingId,enabled:x.enabled,experience_level:x.experienceLevel},{onConflict:'photographer_id,service_offering_id'}).select().single();if(error)throw error;return NextResponse.json(data)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Request failed'},{status:400})}}
