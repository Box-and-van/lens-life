# Antigravity handover
Work from `staging`. Do not modify `main` without an explicit production promotion decision. Supabase staging project reference is `igbhicheuwmmoidxofxk`. Treat the SQL migrations in `supabase/migrations` as the reproducible database source of truth. External provider flags are expected to remain false until credentials and acceptance tests are complete.
